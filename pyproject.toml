[project]
name = "prompt_optimizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["reflex"]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
